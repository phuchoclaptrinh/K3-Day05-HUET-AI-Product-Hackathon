# Copy to codebase/.env or repo-root .env — NEVER commit real keys
GEMINI_API_KEY=
# hoặc
GOOGLE_API_KEY=
OPENAI_API_KEY=

# force mode: auto | gemini | openai | mock
LEARNING_ENGINE_MODE=auto

# models
GEMINI_MODEL=gemini-2.5-flash
OPENAI_MODEL=gpt-4o-mini
